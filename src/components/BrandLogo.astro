---
export interface Props {
  size?: 'sm' | 'md' | 'lg';
  class?: string;
  priority?: boolean;
  alt?: string;
  itemprop?: string;
  ariaHidden?: boolean;
}

const {
  size = 'md',
  class: className = '',
  priority = false,
  alt = 'Buck Strong Garage Doors logo',
  itemprop,
  ariaHidden = false
} = Astro.props;

const widthMap = {
  sm: 72,
  md: 120,
  lg: 160
} as const;

const baseWidth = widthMap[size];
const aspectRatio = 1;
const baseHeight = Math.round(baseWidth * aspectRatio);

const decoding = priority ? 'sync' : 'async';
const fetchPriority = priority ? 'high' : 'auto';

const sizes =
  size === 'sm'
    ? '(max-width: 640px) 40px, 48px'
    : size === 'lg'
      ? '(max-width: 768px) 72px, (max-width: 1280px) 96px, 112px'
      : '(max-width: 640px) 56px, (max-width: 1024px) 72px, 96px';
---
<img
  src="/images/ohio%20logo.png"
  alt={alt}
  width={baseWidth}
  height={baseHeight}
  class={`w-auto ${className}`.trim()}
  loading="eager"
  decoding={decoding}
  fetchpriority={fetchPriority}
  sizes={sizes}
  itemprop={itemprop}
  aria-hidden={ariaHidden ? 'true' : undefined}
/>
